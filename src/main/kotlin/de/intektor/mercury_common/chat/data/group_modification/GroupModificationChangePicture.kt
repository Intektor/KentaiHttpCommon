package de.intektor.mercury_common.chat.data.group_modification

import java.util.*

/**
 * @author Intektor
 */
class GroupModificationChangePicture(val referenceUUID: UUID, chatUUID: UUID, modificationUUID: UUID) : GroupModification(chatUUID, modificationUUID)